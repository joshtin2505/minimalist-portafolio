---
    const { title } = Astro.props
---
<section>
   { title && <h2>{title}</h2> }
   <slot/>
</section>
<style>
    section{
        max-width: 700px;
        margin: 0 auto 72px; 
    }
    h2 {
        font-size: 1.5rem;
        margin-bottom: 8px;
        font-weight: 700;
        line-height: 1.5;
    }
</style>